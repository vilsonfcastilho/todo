[package]
name = "todo"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.29.1"
egui = "0.29.1"

[profile.release]
panic = "abort"

[package.metadata.windows]
windows_subsystem = "windows"
